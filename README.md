# Introduction to Git and GitHub

## 🧭 أوامر موجّه الأوامر (CMD Commands)

### أوامر CMD الأساسية

| الأمر | مهمّته |
|-------|--------|
| `pwd` | معرفة المجلد الذي نعمل به حاليًا |
| `cd` | تغيير المجلد الذي نعمل فيه |
| `cd ..` | الرجوع للمجلد الذي يحتوي على المجلد الذي نحن فيه |
| `ls` | عرض المجلدات والملفات |
| `mkdir` | إنشاء مجلد جديد |
| `clear` | تنظيف الشاشة |

---

# 🟦 جميع أوامر Git الأساسية

## 🔹 تهيئة مستودع Git
| الأمر | الوصف |
|------|--------|
| `git init` | إنشاء مستودع Git جديد داخل المجلد الحالي |
| `git clone <url>` | نسخ مستودع من GitHub إلى جهازك |

---

## 🔹 فحص الحالة ومعرفة التغييرات
| الأمر | الوصف |
|------|--------|
| `git status` | معرفة حالة الملفات (معدّلة / غير متتبّعة) |
| `git diff` | عرض التعديلات التي لم تُضف بعد |

---

## 🔹 إضافة الملفات (Staging Area)
| الأمر | الوصف |
|------|--------|
| `git add filename` | إضافة ملف محدد |
| `git add .` | إضافة كل الملفات المعدّلة |
| `git reset filename` | إزالة ملف من الـ staging قبل الـ commit |

---

## 🔹 إنشاء نسخه محفوظة (Commit)
| الأمر | الوصف |
|------|--------|
| `git commit -m "message"` | إنشاء commit مع رسالة |
| `git commit -a -m "message"` | إضافة + commit للملفات المتتبّعة فقط |

---

## 🔹 سجل التغييرات
| الأمر | الوصف |
|------|--------|
| `git log` | عرض جميع الـ commits |
| `git log --oneline` | عرض سجل مختصر |

---

## 🔹 التعامل مع الفروع (Branches)
| الأمر | الوصف |
|------|--------|
| `git branch` | عرض الفروع |
| `git branch name` | إنشاء فرع جديد |
| `git checkout name` | الانتقال لفرع معين |
| `git checkout -b name` | إنشاء + الانتقال لفرع جديد |

---

## 🔹 الدمج (Merge)
| الأمر | الوصف |
|------|--------|
| `git merge name` | دمج فرع داخل الفرع الحالي |
| `git merge --abort` | إلغاء عملية الدمج عند حدوث تعارض |

---

## 🔹 التعامل مع GitHub (Remote)
| الأمر | الوصف |
|------|--------|
| `git remote -v` | عرض المستودعات المرتبطة |
| `git remote add origin <url>` | إضافة remote جديد |
| `git remote remove origin` | حذف remote |
| `git remote set-url origin <url>` | تغيير رابط remote |

---

## 🔹 رفع التغييرات إلى GitHub
| الأمر | الوصف |
|------|--------|
| `git push` | رفع التغييرات |
| `git push -u origin master` | لأول مرة فقط (ربط الفرع) |
| `git push --force` | رفع مع استبدال الملفات بالقوة **(خطر)** |

---

## 🔹 سحب التغييرات من GitHub
| الأمر | الوصف |
|------|--------|
| `git pull` | سحب آخر التحديثات |
| `git fetch` | جلب التحديثات بدون دمجها |

---

## 🔹 إعادة تسمية أو نقل الملفات
| الأمر | الوصف |
|------|--------|
| `git mv old new` | إعادة تسمية أو نقل ملف عبر Git |

---

# 🎯 ملاحظات
هذا المستودع يمثّل مقدمة عملية في Git وGitHub، ويحتوي على أهم الأوامر للمبتدئين والمتقدمين.

---

---

## 🎯 أهداف التعلم (Learning Objectives)

من خلال هذا المشروع ستتعلّم أساسيات Git و GitHub بشكل عملي، وتشمل:

- فهم مفهوم أنظمة التحكم بالنسخ (Version Control) ولماذا تُستخدم.
- إنشاء مستودع Git جديد والعمل داخل المسارات (Directories).
- تتبّع الملفات باستخدام أوامر Git الأساسية مثل add و commit و status.
- إدارة سجل التغييرات (History) باستخدام git log و git diff.
- التعامل مع الفروع (Branches) والانتقال بينها وتنفيذ عمليات الدمج (Merge).
- ربط المستودع المحلي بـ GitHub باستخدام أوامر remote.
- رفع التعديلات إلى GitHub باستخدام git push وسحب التحديثات باستخدام git pull.
- حل المشكلات الشائعة مثل تعارضات الدمج (Merge Conflicts) وأخطاء push.
- تنظيم الملفات وإعادة تسميتها باستخدام git mv للحفاظ على تاريخها.
- كتابة ملف README احترافي واستخدام Markdown لعرض الجداول والأقسام.

هذه الأهداف ستمنحك أساسًا قويًا لفهم Git وتطبيقه بشكل صحيح في المشاريع الواقعية.

---

# ✨ Created by **Saleh Mahbub**